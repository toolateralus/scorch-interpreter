foreach := (array:Array, callback:Fn) {
    repeat i < (len(array)) {
        callback(array[i], i)
    }
}

callback := (item:Int, index:Double) {
    println(item, index)
}

var array := [0,1,2,3,4,5,6]
 
array.foreach(callback)