arr : Array = [] 

State : class = |
    current : String,
    index : UInt,
    error: Bool, 
|

state := new State()

iter := (arr : Array, str : String) {
    if state.error {
        return state.get_error()
    }
    if state.current >= state.length {
        state.complete = true
        return state.get()
    }
    return state.get()
}

func := (str : String) {
    
}